# The Vite dev server proxies /api and /uploads to the backend (see vite.config.ts).
# In production, build this app as static files and serve them from the same origin
# as the backend (or configure your reverse proxy to route /api and /uploads to it) —
# no build-time API base URL is needed.
